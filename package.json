{
  "name": "protocolo-web3-u1c5",
  "version": "1.0.0",
  "description": "MVP Web3 com ERC-20, NFT, staking, governanca, oraculo e scripts ethers.js.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "interact:sepolia": "hardhat run scripts/interact.js --network sepolia",
    "audit:slither": "slither .",
    "audit:mythril": "myth analyze contracts/StakingRewards.sol"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.15"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.2.0",
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.13.4"
  }
}
